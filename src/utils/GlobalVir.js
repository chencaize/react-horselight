const DIRECTION = {
    LEFT: "left",
    RIGHT: "right"
}
const MILLISEC = 60;//移动定时器,执行速度 60ms一次

const DEFAULT_CLASSNAME = ""//自定义类名
const DEFAULT_DATA = [];//要展示的数据
const DEFAULT_SPACE = 10;//每个元素的间隔
const DEFAULT_DIRECTION = DIRECTION.LEFT;//移动方向 left|right
const DEFAULT_DELAYTIME = 5000;//延迟时间 ms
const DEFAULT_HEIGHT = 30;//容器高度
const DEFAULT_WIDTH = "100%";//容器宽度
const DEFAULT_DISPLAYCOUNT = 4;//展示区域个数
const DEFAULT_SPEED = 1000;//单次移动速度 ms
const DEFAULT_STEP = 1;//单次移动步长

export {
    DIRECTION,
    MILLISEC,
    DEFAULT_CLASSNAME,
    DEFAULT_DATA,
    DEFAULT_SPACE,
    DEFAULT_DIRECTION,
    DEFAULT_DELAYTIME,
    DEFAULT_HEIGHT,
    DEFAULT_WIDTH,
    DEFAULT_DISPLAYCOUNT,
    DEFAULT_SPEED,
    DEFAULT_STEP
}